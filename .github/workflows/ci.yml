name: verify project

on: [push, pull_request]

jobs:
  install:

    runs-on: ubuntu-24.04

    steps:
    - uses: actions/checkout@v6

    - name: Setup PHP
      uses: shivammathur/setup-php@v2
      with:
        php-version: '8.5'

    - name: Validate composer.json and composer.lock
      run: composer validate

    - name: Cache Composer packages
      id: composer-cache
      uses: actions/cache@v5
      with:
        path: vendor
        key: composer-lock-${{ hashFiles('**/composer.lock') }}

    - name: Install composer dependencies
      if: steps.composer-cache.outputs.cache-hit != 'true'
      run: composer install --prefer-dist --no-progress --no-suggest

    - name: Cache phive packages
      id: phive-tools-cache
      uses: actions/cache@v5
      with:
        path: tools
        key: phive-tools-${{ hashFiles('**/.phive/phars.xml') }}

    - name: Install phive tools
      if: steps.phive-tools-cache.outputs.cache-hit != 'true'
      run: wget -O phive https://phar.io/releases/phive.phar && chmod +x phive && ./phive install --trust-gpg-keys 4AA394086372C20A,99BF4D9A33D65E1E,6DA3ACC4991FFAE5,C5095986493B4AA0 && chmod -R +x ./tools/

    - name: 'Tar project'
      run: tar -cf project.tar .

    - uses: actions/upload-artifact@v7
      with:
        name: installed-project
        path: project.tar

  phplint:
    needs: install
    runs-on: ubuntu-24.04

    steps:
    - uses: actions/download-artifact@v8
      with:
        name: installed-project

    - name: Untar project.
      run:  tar -xf project.tar

    - name: Setup PHP
      uses: shivammathur/setup-php@v2
      with:
        php-version: '8.5'

    - name: Run php lint
      run: find ./src/ ./tests/ -type f -name '*.php' -print0 | xargs -0 -n1 -P4 php -l -n | (! grep -v "No syntax errors detected" )

  test:
    needs: phplint
    runs-on: ubuntu-24.04

    steps:
    - uses: actions/download-artifact@v8
      with:
        name: installed-project

    - name: Untar project.
      run:  tar -xf project.tar

    - name: Setup PHP with Xdebug
      uses: shivammathur/setup-php@v2
      with:
        php-version: '8.5'
        coverage: xdebug

    - name: Run PHPUnit test
      run: ./tools/phpunit -c ./ci/phpunit.xml

  static-analysis:
    needs: phplint
    runs-on: ubuntu-24.04

    steps:
    - uses: actions/download-artifact@v8
      with:
        name: installed-project

    - name: Untar project.
      run:  tar -xf project.tar

    - name: Setup PHP
      uses: shivammathur/setup-php@v2
      with:
        php-version: '8.5'

    - name: Run Psalm
      run: ./tools/psalm -c ./ci/psalm.xml --shepherd --no-cache
